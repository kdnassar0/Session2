{% extends 'base.html.twig' %}

{% block title %}Hello SessionController!{% endblock %}

{% block body %}
<h1>List des sessions Passees</h1>
{% for session in SessionsPassees %}
    {{session.formation }}
    <a href="">{{ session.nomSession}} </a>
    {{session.dateDebut | date ('Y-m-d') }}
    {{ session.dateFin | date ('Y-m-d') }}
    {{ session.nbPlaceTotal }}  <br>
{% endfor %}
<h1>List des sessions Encours</h1>
{% for session in SessionsEncours %}
    {{session.formation }}
    <a href="">{{ session.nomSession}} </a>
    {{session.dateDebut | date ('Y-m-d') }}
    {{ session.dateFin | date ('Y-m-d') }}
    {{ session.nbPlaceTotal }}  <br>
{% endfor %}
<h1>List des sessions A venir</h1>
{% for session in SessionAvenir %}
    {{session.formation }}
    <a href="">{{ session.nomSession}} </a>
    {{session.dateDebut | date ('Y-m-d') }}
    {{ session.dateFin | date ('Y-m-d') }}
    {{ session.nbPlaceTotal }}  <br>
{% endfor %}
{% endblock %}
